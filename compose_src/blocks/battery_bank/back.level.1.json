{
  "$includes": ["_variables", "blocks/battery_bank/_back_layers"],
  "output": "blocks/battery_bank/back.level.1",
  "variables": {
    "gauge_color": {"$variable": "error_color"}
  },
  "frames": [
    {"layers": ["base", "gauge.1"]}
  ]
}