{
  "$includes": ["_variables", "blocks/battery_bank/_front_layers"],
  "output": "blocks/battery_bank/front.level.1",
  "variables": {
    "gauge_color": {"$variable": "error_color"}
  },
  "frames": [
    {"layers": ["base", "gauge.1"]}
  ]
}